{
  "name": "web-socket",
  "module": "index.ts",
  "type": "module",
  "private": true,

  "scripts": {
    "build":"bunx tsc -b",
    
    "start": "bun dist/index.js",
    "dev":"bun run build && bun run start"
  },

  "devDependencies": {
    "@types/bun": "latest",
    "@repo/typescript-config": "workspace:*",
     "typescript": "latest",
     "@repo/db":"workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
